#!/usr/bin/env groovy

//TODO we can add a pipeline to cut a release (tag+commit to the repo) if versioning strategy is not to release/deploy on any new commit
//(i.e. only collections of commits make up a meaningful moment to release)
