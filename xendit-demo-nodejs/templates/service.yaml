apiVersion: v1
kind: Service
metadata:
  name: {{ include "xendit-demo-nodejs.fullname" . }}
  labels:
    {{- include "xendit-demo-nodejs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "xendit-demo-nodejs.selectorLabels" . | nindent 4 }}
